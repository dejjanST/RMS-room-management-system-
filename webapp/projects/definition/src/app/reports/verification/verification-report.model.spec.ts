import { VerificationReport } from './verification-report.model';

describe('VerificationReport', () => {
  it('should create an instance', () => {
    expect(new VerificationReport()).toBeTruthy();
  });
});
